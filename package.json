{
  "name": "global-resilience-os",
  "version": "1.0.0-local",
  "private": true,
  "type": "module",
  "description": "Local release workspace for Global Resilience OS",
  "scripts": {
    "test": "npm --prefix backend test",
    "lint": "npm --prefix frontend run lint",
    "build": "npm --prefix frontend run build",
    "verify": "node scripts/local-release-evidence.js",
    "verify:install": "node scripts/local-installation-check.js",
    "start": "npm --prefix backend start",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
